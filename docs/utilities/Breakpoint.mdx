---
name: Breakpoint
menu: Utilities
---

import { Playground } from 'docz'
import { Props, PropDesc } from '@docs/utils'
import { Button, Breakpoint } from '@smooth-ui/core-sc'

# Breakpoint

Breakpoint is an utility to show or hide a block relatively to a breakpoint.

<Playground>
  <Breakpoint down="md">
    <Button variant="primary">Button displayed down to md</Button>
  </Breakpoint>
  <Breakpoint up="md">
    <Button variant="secondary">Button displayed up to md</Button>
  </Breakpoint>
</Playground>

## API

### Toggler

<Props
  props={PropDesc({
    children: PropDesc.func.isRequired,
    down: PropDesc.string,
    up: PropDesc.string,
  })}
/>
